# 怎麼開本機預覽（給 Chofi 的小抄）

## 每次要看成果，做這 3 步

1. 打開終端機（Terminal），進到這個專案資料夾
2. 輸入這行，按 Enter：

   ```
   npm run dev
   ```

3. 看到這行就成功了：

   ```
   ┃ Local    http://localhost:4321/
   ```

   打開瀏覽器，貼上 **http://localhost:4321/** 就能看。

> 在 VSCode 裡更快：上方選單 Terminal → New Terminal，會自動在專案資料夾，直接打 `npm run dev` 就好。

---

## 幾個一定要知道的點

- **網址是 4321，不是 3000。** 這個專案固定用 4321。
- **要邊看邊改**：改完 code 存檔，瀏覽器會自己更新，不用重開。
- **看完想關掉**：回到終端機，按 `Ctrl + C`。
- **網址連不上 = 程式沒在跑。** 重做上面 3 步即可。localhost 只在 `npm run dev` 運行時存在，關終端機或關電腦它就停了，這是正常的。

---

## 看哪個檔案？

- 真正的 hi-fi 首頁 = Astro 在跑的版本，請用 **http://localhost:4321/**（檔案在 `src/`）。
- 根目錄那些單獨的 `.html`（例如 `index.html`）是舊的靜態檔，可以直接點開，但不是現在在做的版本。
